<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Funding (Dev) | Olympia Did That</title>

  <style>
    :root{
      --overlay: 0.22;
      --stroke: rgba(255,255,255,0.18);
      --stroke2: rgba(255,255,255,0.26);
      --text: rgba(255,255,255,0.94);
      --muted: rgba(255,255,255,0.78);
      --shadow: 0 18px 50px rgba(0,0,0,0.50);
      --radius: 18px;
      --max: 1180px;

      --panelBg: rgba(0,0,0,0.70);
      --panelBg2: rgba(0,0,0,0.62);

      --red: #ff3b3b;
      --green: #45ff9a;
      --amber: #ffcc66;
      --blue: #66ccff;
    }

    *{ box-sizing:border-box; }
    html, body{ height:100%; }

    body{
      margin:0;
      font-family: Arial, system-ui, -apple-system, Segoe UI, Roboto, sans-serif;
      color: var(--text);
      background:
        linear-gradient(rgba(0,0,0,var(--overlay)), rgba(0,0,0,var(--overlay))),
        url("/assets/money-bg.jpg") center / cover no-repeat fixed;

      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    a{ color:inherit; text-decoration:none; }
    a:hover{ text-decoration:underline; }

    #site-header{ position: sticky; top:0; z-index:1000; }
    main{ flex: 1 0 auto; }

    .wrap{ max-width:var(--max); margin:0 auto; padding:20px; }

    .hero{ padding: 22px 0 10px; }
    .heroGrid{
      display:grid;
      grid-template-columns: 1.15fr 0.85fr;
      gap: 16px;
      align-items: stretch;
    }

    .panel{
      border: 1px solid var(--stroke);
      border-radius: var(--radius);
      background: var(--panelBg);
      box-shadow: var(--shadow);
      overflow:hidden;
    }
    .panelPad{ padding: 22px; }

    h1{
      margin:0 0 10px;
      font-size: clamp(30px, 3.2vw, 46px);
      line-height:1.05;
      text-transform: uppercase;
      letter-spacing:0.6px;
    }

    .sub{
      margin:0;
      color:var(--muted);
      line-height:1.6;
      max-width: 80ch;
    }

    .tiny{ color:var(--muted); font-size:13px; }

    /* Progress */
    .progressWrap{ margin-top: 14px; }
    .bar{
      height:12px;
      border-radius:999px;
      border:1px solid var(--stroke);
      background:rgba(0,0,0,0.55);
      overflow:hidden;
      margin-top:8px;
    }
    .bar > div{
      height:100%;
      width: 0%;
      background: rgba(255,59,59,0.92);
      transition: width .35s ease;
    }

    /* Donation Options */
    .choice{
      display:flex;
      gap:10px;
      align-items:center;
      padding:10px 12px;
      border:1px solid var(--stroke);
      background:rgba(255,255,255,0.12);
      border-radius:14px;
      transition: transform .12s ease, box-shadow .12s ease, border-color .12s ease;
    }
    .choice:hover{
      transform: translateY(-1px);
      box-shadow: 0 10px 26px rgba(0,0,0,0.25);
    }

    /* Make the $50/mo option visually dominant */
    .choice.featured{
      border-color: rgba(255,59,59,0.55);
      box-shadow:
        0 0 0 1px rgba(255,59,59,0.18),
        0 18px 50px rgba(0,0,0,0.35);
      background: rgba(255,255,255,0.14);
      position: relative;
    }
    .badge{
      position:absolute;
      right: 12px;
      top: 10px;
      font-size: 11px;
      font-weight: 900;
      letter-spacing: .6px;
      text-transform: uppercase;
      padding: 6px 10px;
      border-radius: 999px;
      border: 1px solid rgba(255,255,255,0.18);
      background: rgba(255,59,59,0.92);
      color: #111;
    }

    .btnRow{
      display:flex;
      gap:12px;
      flex-wrap:wrap;
      margin-top: 16px;
      align-items:center;
    }

    .btn{
      display:inline-flex;
      align-items:center;
      justify-content:center;
      padding: 12px 14px;
      border-radius: 14px;
      border: 1px solid var(--stroke2);
      background: rgba(255,255,255,0.14);
      font-weight: 900;
      letter-spacing: 0.4px;
      text-transform: uppercase;
      cursor:pointer;
      box-shadow: 0 10px 26px rgba(0,0,0,0.35);
      transition: background .15s ease, box-shadow .15s ease, filter .15s ease, transform .15s ease;
      user-select:none;
    }

    .btnPrimary{
      background: rgba(255,59,59,0.92);
      border-color: rgba(255,59,59,0.92);
      color:#111;
    }

    .btn:hover{
      filter: brightness(1.07);
      transform: translateY(-1px);
      text-decoration:none;
      box-shadow:
        0 0 0 1px rgba(255,255,255,0.18),
        0 14px 34px rgba(0,0,0,0.45);
    }

    /* Dev tools */
    .devPanel{
      margin-top: 16px;
      border-top: 1px solid rgba(255,255,255,0.12);
      padding-top: 14px;
    }
    .devTitle{
      display:flex;
      align-items:center;
      justify-content:space-between;
      gap:10px;
      margin-bottom:10px;
    }
    .pill{
      display:inline-flex;
      align-items:center;
      gap:8px;
      border-radius: 999px;
      border: 1px solid rgba(255,255,255,0.16);
      background: rgba(255,255,255,0.10);
      padding: 8px 10px;
      font-size: 12px;
      color: var(--muted);
      overflow-wrap:anywhere;
    }
    .pill strong{ color: var(--text); }

    .grid2{
      display:grid;
      grid-template-columns: 1fr 1fr;
      gap: 10px;
    }
    .input{
      width:100%;
      padding: 10px 12px;
      border-radius: 12px;
      border: 1px solid rgba(255,255,255,0.18);
      background: rgba(0,0,0,0.35);
      color: var(--text);
      outline:none;
    }
    .input::placeholder{ color: rgba(255,255,255,0.55); }

    .devBtnRow{ display:flex; gap:10px; flex-wrap:wrap; margin-top:10px; }
    .btnSmall{
      padding: 10px 12px;
      border-radius: 12px;
      font-size: 12px;
    }
    .btnGhost{
      background: rgba(255,255,255,0.10);
      border-color: rgba(255,255,255,0.18);
    }
    .btnBlue{
      background: rgba(102,204,255,0.88);
      border-color: rgba(102,204,255,0.88);
      color:#111;
    }
    .btnGreen{
      background: rgba(69,255,154,0.88);
      border-color: rgba(69,255,154,0.88);
      color:#111;
    }

    @media (max-width: 980px){
      .heroGrid{ grid-template-columns: 1fr; }
      .grid2{ grid-template-columns: 1fr; }
    }
  </style>
</head>

<body>
  <div id="site-header"></div>

  <main class="wrap hero">
    <div class="heroGrid">

      <!-- LEFT -->
      <section class="panel panelPad">
        <h1>Funding (Dev)</h1>
        <p class="sub">
          Trial sandbox page for donation UX. This page pre-fills Anedot via URL parameters and
          shows exactly what link will be opened.
        </p>

        <div class="progressWrap">
          <div class="tiny" id="progressText">Goal: 2,000 donors at $50/mo. Current: 12%</div>
          <div class="bar"><div id="barFill"></div></div>
        </div>

        <div style="margin-top:16px;">
          <div style="font-weight:900; text-transform:uppercase; letter-spacing:0.4px;">
            Recommended: $50/month for 12 months
          </div>
          <div class="tiny" style="margin-top:6px;">
            A $600 investment in Washington’s future.
          </div>
        </div>

        <!-- DEV CONTROLS -->
        <div class="devPanel">
          <div class="devTitle">
            <div style="font-weight:900; text-transform:uppercase; letter-spacing:0.4px;">Dev Tools</div>
            <div class="pill"><strong>Mode:</strong> Prefill Builder</div>
          </div>

          <div class="grid2">
            <input class="input" id="prefillEmail" placeholder="email (optional, avoid PII in production links)" autocomplete="email">
            <input class="input" id="prefillZip" placeholder="zip/postal_code (optional)" inputmode="numeric" autocomplete="postal-code">
          </div>

          <div class="devBtnRow">
            <button class="btn btnSmall btnGhost" id="copyLinkBtn" type="button">Copy Prefill Link</button>
            <button class="btn btnSmall btnBlue" id="openSameTabBtn" type="button">Open (same tab)</button>
            <button class="btn btnSmall btnGreen" id="openNewTabBtn" type="button">Open (new tab)</button>
          </div>

          <div style="margin-top:10px;" class="pill">
            <strong>Computed URL:</strong>
            <span id="computedUrl">—</span>
          </div>

          <div class="tiny" style="margin-top:10px;">
            Note: putting email/PII in query strings can end up in analytics logs. Use only for dev tests.
          </div>
        </div>

      </section>

      <!-- RIGHT -->
      <aside class="panel panelPad">
        <div style="font-weight:900; text-transform:uppercase; letter-spacing:0.4px;">
          Donation Options
        </div>

        <div style="display:grid; gap:10px; margin-top:12px;">
          <label class="choice">
            <input type="radio" name="amt" value="5">
            $5 Grain of Salt (Supporter)
          </label>

          <label class="choice">
            <input type="radio" name="amt" value="20">
            $20 Budget (most popular)
          </label>

          <label class="choice featured" id="featuredChoice">
            <span class="badge">Most Effective</span>
            <input type="radio" name="amt" value="50" checked>
            $50 Monthly (Recommended)
          </label>

          <label class="choice">
            <input type="radio" name="amt" value="600">
            $600 One-Time Donation (Annual Impact)
          </label>
        </div>

        <div class="btnRow">
          <a class="btn btnPrimary" id="donateBtn"
             href="https://secure.anedot.com/pu4wa/donate-odt"
             target="_blank" rel="noopener">
            Fund the Truth
          </a>

          <a class="btn" href="/action.html">
            Back to Take Action
          </a>
        </div>

        <div class="tiny" style="margin-top:12px;">
          Secure donation processing provided by People United for Washington.
        </div>
      </aside>

    </div>
  </main>

  <div id="site-footer"></div>

  <script>
    /********************
     * Header/Footer inject
     ********************/
    async function inject(selector, url){
      const el = document.querySelector(selector);
      if (!el) return;
      try{
        const res = await fetch(url, { cache: "no-cache" });
        if (!res.ok) throw new Error(`${res.status} ${res.statusText}`);
        el.innerHTML = await res.text();
      }catch(err){
        console.warn("Inject failed:", url, err);
        el.innerHTML = "";
      }
    }

    /********************
     * Funding dev settings
     ********************/
    const ANEDOT_BASE = "https://secure.anedot.com/pu4wa/donate-odt";

    function selectedAmount(){
      return (document.querySelector('input[name="amt"]:checked')?.value || "50");
    }

    function safeTrim(v){ return (v || "").trim(); }

    function addTrackingParams(url){
      url.searchParams.set("utm_source", "odt");
      url.searchParams.set("utm_medium", "website");
      url.searchParams.set("utm_campaign", "funding_dev");
      // If your Anedot account supports this, it’s useful:
      // url.searchParams.set("source_code", "odt_funding_dev");
      return url;
    }

    function buildAnedotUrl(){
      const amt = selectedAmount();
      const url = new URL(ANEDOT_BASE);

      url.searchParams.set("amount", amt);

      // Monthly for the $50 radio, otherwise one-time
      if (amt === "50") url.searchParams.set("frequency", "monthly");
      else url.searchParams.delete("frequency");

      // Optional dev-only PII prefill (avoid in production links)
      const email = safeTrim(document.getElementById("prefillEmail")?.value);
      const zip = safeTrim(document.getElementById("prefillZip")?.value);

      if (email) url.searchParams.set("email", email);
      else url.searchParams.delete("email");

      // Anedot URL param for ZIP is typically `postal_code`
      if (zip) url.searchParams.set("postal_code", zip);
      else url.searchParams.delete("postal_code");

      addTrackingParams(url);
      return url.toString();
    }

    function setProgress(pct){
      const fill = document.getElementById("barFill");
      if (!fill) return;
      const clamped = Math.max(0, Math.min(100, pct));
      fill.style.width = clamped + "%";
      const txt = document.getElementById("progressText");
      if (txt) txt.textContent = `Goal: 2,000 donors at $50/mo. Current: ${clamped}%`;
    }

    function updateDonateLabelAndLink(){
      const btn = document.getElementById("donateBtn");
      if (!btn) return;

      const amt = selectedAmount();
      if (amt === "50") btn.textContent = "Fund the Truth ($50/mo)";
      else if (amt === "600") btn.textContent = "Fund the Truth ($600)";
      else btn.textContent = `Fund the Truth ($${amt})`;

      const u = buildAnedotUrl();
      btn.href = u;

      const out = document.getElementById("computedUrl");
      if (out) out.textContent = u;
    }

    async function copyText(text){
      try{
        await navigator.clipboard.writeText(text);
        return true;
      }catch(e){
        return false;
      }
    }

    (async () => {
      await inject("#site-header", "/partials/header.html");
      await inject("#site-footer", "/partials/footer.html");

      // Default progress
      setProgress(12);

      // Bind changes
      document.querySelectorAll('input[name="amt"]').forEach(r => {
        r.addEventListener("change", updateDonateLabelAndLink);
      });

      ["prefillEmail","prefillZip"].forEach(id => {
        const el = document.getElementById(id);
        if (el) el.addEventListener("input", updateDonateLabelAndLink);
      });

      // Dev buttons
      document.getElementById("openSameTabBtn")?.addEventListener("click", () => {
        window.location.href = buildAnedotUrl();
      });

      document.getElementById("openNewTabBtn")?.addEventListener("click", () => {
        window.open(buildAnedotUrl(), "_blank", "noopener");
      });

      document.getElementById("copyLinkBtn")?.addEventListener("click", async () => {
        const u = buildAnedotUrl();
        const ok = await copyText(u);
        const btn = document.getElementById("copyLinkBtn");
        if (!btn) return;
        const old = btn.textContent;
        btn.textContent = ok ? "Copied ✅" : "Copy failed ❌";
        setTimeout(() => btn.textContent = old, 1200);
      });

      updateDonateLabelAndLink();
    })();
  </script>

  <script src="/assets/site.js"></script>
</body>
</html>
